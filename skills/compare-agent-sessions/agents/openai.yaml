interface:
  display_name: "Compare Agent Sessions"
  short_description: "Compare two local coding-agent runs safely"
  default_prompt: "Use $compare-agent-sessions to compare my two latest local Codex sessions safely."
